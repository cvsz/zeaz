# Technical Debt

1. Bind SCB OAuth callback to one-time state/PKCE and expire replayed state.
2. Add bounded worker pools/request timeouts for direct-origin slowloris protection.
3. Complete delivery/inventory transition invariants and add concurrency tests.
4. Wire dependency/security audits into CI and add a measured AI fallback benchmark.
