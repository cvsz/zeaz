# Finance workspace

Keep business-specific financial models outside source templates. Start by
copying `excel/financial-model.csv` into a business kit, then replace the
default assumptions with local, verified values.

This framework does not provide tax, legal, lending or investment advice.
