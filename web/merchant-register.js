const form=document.querySelector("#merchant-register"),result=document.querySelector("#result");
form.addEventListener("submit",async event=>{event.preventDefault();const button=form.querySelector("button");button.disabled=true;try{const response=await fetch("/api/merchants/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||"ส่งใบสมัครไม่สำเร็จ");result.hidden=false;result.textContent=`ส่งใบสมัครแล้ว เลขที่ ${data.application.id} · รอทีมงานตรวจสอบ`;form.reset();}catch(error){result.hidden=false;result.textContent=error.message;}finally{button.disabled=false;}});
