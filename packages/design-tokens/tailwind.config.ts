import { breakpoints, colors, radius, spacing } from "./src/index.js";
export const moopiewPreset = { theme: { extend: { colors, borderRadius: radius, spacing, screens: breakpoints } } };
export default moopiewPreset;
