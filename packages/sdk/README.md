# @moopiew/sdk

Typed, fetch-based API client with request IDs, retry handling and normalized errors. Instantiate `MoopiewClient` with a trusted base URL and a token store; use the domain services with that client.
