import type { Order } from "@moopiew/types"; import type { MoopiewClient } from "./client.js"; export class OrdersService { constructor(private client:MoopiewClient){} list(){return this.client.request<Order[]>("/api/orders");} get(id:string){return this.client.request<Order>(`/api/orders/${encodeURIComponent(id)}`);} update(id:string,input:Partial<Pick<Order,"status">>){return this.client.request<Order>(`/api/orders/${encodeURIComponent(id)}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(input)});} }
