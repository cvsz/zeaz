# Dependency Report

Runtime dependencies are intentionally small (`cryptography` plus Python standard library). npm production audit and Python `pip check` are clean as of 2026-07-28. CI build/typecheck and package workspace validation pass.
