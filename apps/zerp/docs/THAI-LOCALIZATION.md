# Thai localization

The UI uses Thai-first labels where the operational meaning is user-facing and
formats monetary values as THB with the `th-TH` locale. The runtime timezone is
`Asia/Bangkok`. Tax invoice legality, withholding forms, Thai date rendering,
and payroll calculations remain deferred pending accountant/legal approval and
dedicated backend models.
