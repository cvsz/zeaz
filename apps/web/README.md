# @moopiew/web

React/Vite migration shell. It consumes the same `/api/menu` contract as the
live Python preorder service; set `VITE_API_URL=https://moopiew.zeaz.dev` when
running it on a different origin.
