services:
  engineering-dashboard:
    build:
      context: ..
      dockerfile: dashboard/Dockerfile
    ports:
      - "127.0.0.1:8080:8080"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
